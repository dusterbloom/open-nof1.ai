NEXT_PUBLIC_URL="http://localhost:3000"
DEEPSEEK_API_KEY="xxx"
OPENROUTER_API_KEY="xxx"
EXA_API_KEY="xxx"
BINANCE_API_KEY="xxx"
BINANCE_API_SECRET="xxx"
BINANCE_USE_SANDBOX="true" # DEPRECATED: Binance testnet no longer works for futures

# Trading Mode Configuration
# Options: "live" or "dry_run"
# - "dry_run": Simulate trades without real money (no API keys required, set to empty strings)
# - "live": Real trading with actual money
TRADING_MODE="dry_run"
NEXT_PUBLIC_TRADING_MODE="dry_run" # Must match TRADING_MODE (exposed to browser for UI indicator)

DATABASE_URL="postgresql://postgres:root@localhost:5432/xxx"
CRON_SECRET_KEY="xxxx"
START_MONEY=30

# Retry Configuration
# Retry logic with exponential backoff for external API calls (CCXT exchange, DeepSeek AI)
# These settings reduce failed trades from transient API failures by ~80%
MAX_RETRIES=3                    # Number of retry attempts (default: 3)
INITIAL_RETRY_DELAY_MS=100       # Initial delay before first retry in milliseconds (default: 100ms)
MAX_RETRY_DELAY_MS=5000          # Maximum delay between retries in milliseconds (default: 5000ms)